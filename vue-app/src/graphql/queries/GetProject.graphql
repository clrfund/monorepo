query GetProject($recipientId: ID!) {
  recipients(where: { id: $recipientId }) {
    id
    requestType
    recipientAddress
    recipientMetadata
    recipientMetadataId
    recipientIndex
    submissionTime
    rejected
    verified
  }
}
