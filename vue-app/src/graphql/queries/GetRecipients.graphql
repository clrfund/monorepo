query GetRecipients($registryAddress: ID!) {
  recipientRegistry(id: $registryAddress) {
    recipients {
      id
      requestType
      requester
      recipientAddress
      recipientMetadata
      submissionTime
      rejected
      verified
    }
  }
}
