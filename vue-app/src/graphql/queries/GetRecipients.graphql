query GetRecipients($registryAddress: String!) {
  recipients(first: 300, where: { recipientRegistry: $registryAddress }) {
    id
    recipientIndex
    requestType
    requester
    recipientAddress
    recipientMetadata
    requestSubmittedHash
    requestResolvedHash
    submissionTime
    rejected
    verified
  }
}
